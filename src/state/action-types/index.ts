export enum ActionType {
  NEXT = 'next',
  PREV = 'previous',
  FIRST = 'first',
  SETABSENCE = 'set',
  SETDEFAULT = 'setdefault',
  SET_TOTAL_REPOSITORIES = 'setTotalRepositories',
  SET_NEW_USER = 'setUser',
  SET_SEARCH_QUERY = 'setSearchQuery'
}

